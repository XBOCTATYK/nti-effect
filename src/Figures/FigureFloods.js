import {Path, Point, Rectangle} from "../paperExports";

class FigureFloods {
  constructor(figures) {
    this.element = new Rectangle(
      new Point(0, 0),
      new Point(1920, 600)
    );

    this.figures = figures;
  }

  animate() {
    
  }
}
